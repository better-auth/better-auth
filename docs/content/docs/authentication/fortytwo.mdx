---
title: 42 School
description: 42 school provider setup and usage.
---

<Steps>
    <Step> 
        ### Get your 42 credentials
        To use 42 sign in, you need an app UID and secret. You can get them from the [intra's developer apps list](https://profile.intra.42.fr/oauth/applications), if you are a student or staff of the school.

        Make sure to set the redirect URL to `http://localhost:3000/api/auth/callback/fortytwo` for local development. For production, you should set it to the URL of your application. If you change the base path of the auth routes, you should update the redirect URL accordingly.
    </Step>

  <Step>
        ### Configure the provider
        To configure the provider, you need to import the provider and pass it to the `socialProviders` option of the auth instance.

        ```ts title="auth.ts" 
        import { betterAuth } from "better-auth"
        
        export const auth = betterAuth({ 
            socialProviders: {
                fortytwo: { // [!code highlight]
                    clientId: process.env.FT_CLIENT_ID as string, // [!code highlight]
                    clientSecret: process.env.FT_CLIENT_SECRET as string, // [!code highlight]
                }, // [!code highlight]
            },
        })
        ```
    </Step>
</Steps>

## Usage

### Sign In with 42

To sign in with 42, you can use the `signIn.social` function provided by the client. The `signIn` function takes an object with the following properties:
- `provider`: The provider to use. It should be set to `fortytwo`.

```ts title="auth-client.ts"
import { createAuthClient } from "better-auth/client"
const authClient =  createAuthClient()

const signIn = async () => {
    const data = await authClient.signIn.social({
        provider: "fortytwo"
    })
}
```

## Options

For the full list of options supported by all social providers, check the [Provider Options](/docs/concepts/oauth#provider-options).
