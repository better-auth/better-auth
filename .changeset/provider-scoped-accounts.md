---
"better-auth": patch
"@better-auth/core": patch
"auth": patch
"@better-auth/sso": patch
---

Identify accounts by `(providerId, accountId)` again, as in Better Auth 1.6, and remove the `issuer` column introduced in 1.7.0.
