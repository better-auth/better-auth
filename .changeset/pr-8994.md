---
"better-auth": patch
---

Add Vercel skew protection (`x-deployment-id`) to client-side auth fetches during rolling releases. Opt out with `disableSkewProtection`; `disableDefaultFetchPlugins` disables all built-in fetch plugins including skew protection.
