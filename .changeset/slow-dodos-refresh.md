---
"@better-auth/refreshable-session": minor
---

Add an opt-in refreshable session plugin with per-client native access lifetimes, rotating refresh tokens, and optional browser recovery that preserves existing web-session behavior by default.
