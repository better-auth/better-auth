---
"better-auth": patch
---

Admin user listing now supports multi-field search through a POST request body, allowing searches across built-in and custom user fields while preserving the existing GET query parameters.
