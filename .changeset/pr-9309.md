---
"better-auth": patch
"@better-auth/core": patch
---

Add `api.rateLimit.customRules.matchOutcome` to allow counting only requests with matching outcomes (for example, failed responses) while preserving existing rate-limit defaults.
