---
"@better-auth/sso": minor
---

Add SSO organization provisioning support for mapping IdP claims (OIDC userinfo responses or verified ID token payloads) or SAML attributes to organization roles during sign-in, including pre-session role sync for existing SSO members.
