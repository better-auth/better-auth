---
"better-auth": patch
---

Adds an optional `cleanupBeforeFind` flag to the internal adapter’s `findVerificationValue` and uses it in `passkey` routes to avoid returning expired verification tokens. Fixes #8376 without changing the default cleanup behavior.
