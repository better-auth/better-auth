---
"better-auth": minor
---

add `sendOldEmailVerification` option to `changeEmail` — when enabled, verifies the new email directly and skips old email confirmation, unblocking users who've lost access to their previous address.
