---
"@better-auth/oauth-provider": minor
---

Add a minimal OAuth Provider extension surface so companion plugins can register token grants, assertion-based client authentication methods, additive discovery metadata, and token/UserInfo claim contributors without changing provider core for each OAuth RFC.
