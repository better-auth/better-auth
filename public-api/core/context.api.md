## API Report File for "@better-auth/core"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { AsyncLocalStorage } from 'node:async_hooks';
import { CookieOptions } from 'better-call';
import { D1Database as D1Database_2 } from '@cloudflare/workers-types';
import { Database } from 'bun:sqlite';
import { DatabaseSync } from 'node:sqlite';
import { Dialect } from 'kysely';
import { Endpoint } from 'better-call';
import { EndpointContext } from 'better-call';
import { InputContext } from 'better-call';
import { Kysely } from 'kysely';
import { Middleware } from 'better-call';
import { Migration } from 'kysely';
import { MysqlPool } from 'kysely';
import { PostgresPool } from 'kysely';
import { SqliteDatabase } from 'kysely';
import { StandardSchemaV1 } from '@standard-schema/spec';
import * as z from 'zod';

// @public (undocumented)
export type AuthEndpointContext = Partial<InputContext<string, any, any, any, any, any> & EndpointContext<string, any, any, any, any, any, any, AuthContext>> & {
    context: AuthContext;
};

// @public
export function defineRequestState<T>(initFn: () => T | Promise<T>): RequestState<T>;

// @public (undocumented)
export function getBetterAuthVersion(): string;

// @public (undocumented)
export const getCurrentAdapter: (fallback: DBTransactionAdapter) => Promise<DBTransactionAdapter>;

// @public (undocumented)
export function getCurrentAuthContext(): Promise<AuthEndpointContext>;

// @public
export function getCurrentAuthContextAsyncLocalStorage(): Promise<AsyncLocalStorage<AuthEndpointContext>>;

// @public
export const getCurrentDBAdapterAsyncLocalStorage: () => Promise<AsyncLocalStorage<HookContext>>;

// @public (undocumented)
export function getCurrentRequestState(): Promise<RequestStateWeakMap>;

// @public (undocumented)
export function getRequestStateAsyncLocalStorage(): Promise<AsyncLocalStorage<RequestStateWeakMap>>;

// @public (undocumented)
export function hasRequestState(): Promise<boolean>;

// @public
export const queueAfterTransactionHook: (hook: () => Promise<void>) => Promise<void>;

// @public (undocumented)
export interface RequestState<T> {
    // (undocumented)
    get(): Promise<T>;
    // (undocumented)
    readonly ref: Readonly<object>;
    // (undocumented)
    set(value: T): Promise<void>;
}

// @public (undocumented)
export type RequestStateWeakMap = WeakMap<object, any>;

// @public (undocumented)
export const runWithAdapter: <R>(adapter: DBAdapter, fn: () => R) => Promise<R>;

// @public (undocumented)
export function runWithEndpointContext<T>(context: AuthEndpointContext, fn: () => T): Promise<T>;

// @public (undocumented)
export function runWithRequestState<T>(store: RequestStateWeakMap, fn: () => T): Promise<T>;

// @public (undocumented)
export const runWithTransaction: <R>(adapter: DBAdapter, fn: () => R) => Promise<R>;

// (No @packageDocumentation comment for this package)

```
